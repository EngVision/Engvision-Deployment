apiVersion: apps/v1
kind: Deployment
metadata:
  name: evs-fe-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: evs-fe-deployment
  template:
    metadata:
      labels:
        app: evs-fe-deployment
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: evs-fe-deployment

      containers:
        - name: evs-fe-deployment
          image: ghcr.io/engvision/project-engvision:frontend-180
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: k8s-ghcr

#       resource:
#         requests:
#           memory: "512Mi"
#           cpu: "250m"
#         limits:
#           memory: "1Gi"
#           cpu: "500m"

# ---
# apiVersion: autoscaling/v2
# kind: HorizontalPodAutoscaler
# metadata:
#   name: evs-fe-deployment
# spec:
#   scaleTargetRef:
#     apiVersion: apps/v1
#     kind: Deployment
#     name: evs-fe-deployment
#   minReplicas: 1
#   maxReplicas: 3
#   metrics:
#     - type: Resource
#       resource:
#         name: cpu
#         target:
#           type: Utilization
#           averageUtilization: 80
#     - type: Resource
#       resource:
#         name: memory
#         target:
#           type: Utilization
#           averageUtilization: 80
