apiVersion: v1
kind: Service
metadata:
  name: evs-whisper-deployment
spec:
  selector:
    app: evs-whisper-deployment
  ports:
    - name: evs-whisper-deployment
      protocol: TCP
      port: 8000
      targetPort: 8000
